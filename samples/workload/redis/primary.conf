# redis-primary — accepts writes from the api.
# Persistence is OFF so every orchestrator runs the identical in-memory profile
# (no disk-tuning variance to bias throughput). The bench is stateless between
# runs by design.
save ""
appendonly no
maxmemory-policy noeviction
# Closed bench network only — no auth, reachable by service name.
protected-mode no
