/jaco
/jacod
/dist/
*.test
*.out
coverage.*

# Benchmark testbed local secrets (Azure creds + SSH key). The rest of
# testbed/ is the tracked, sanitized infra.
testbed/.env.local

# Benchmark run output (summaries, scraped metrics, bundles).
samples/bench/results/

# Node build artifacts in the sample workload (package-lock.json is tracked).
node_modules/
samples/workload/api/dist/

# Editor / agent metadata.
.agents/
.claude/
skills-lock.json

# Original Azure template archive.
template.zip
