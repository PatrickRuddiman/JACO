# Stack-scoped environment file referenced by jaco.yaml's `environment:`.
# Only KEYs referenced via ${VAR} in compose.yml end up in containers;
# anything else here is interpolation-only and never leaks to a service.
REGISTRY=ghcr.io
DB_URL=postgres://db.internal/env-demo
