# Identical mount shape to front.compose.yml — same service name, same
# volume key, same target path. Cluster/deployment/key identities keep
# the resulting `jaco_v2_<digest>` volumes distinct.
services:
  redis:
    image: redis:7-alpine
    volumes:
      - data:/data
volumes:
  data: {}
